#pragma once

#include "include.h"
#include "../invader.h"

/// INT(caster) invader
/// 
/// bad stats, good ability
class INT_invader : public invader {
 public:
	 INT_invader() : invader(5, 3, 4) {

	 }
 private:
};
